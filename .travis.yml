stages:
  - test
  - deploy

language: python
services: mongodb

cache: pip

python:
  - "3.6"

install:
  - pip install -r requirements.txt -e .
  - pip install pytest-cov codecov
script:
  - pytest -v --cov=./gcmcworkflow
after_success:
  - codecov


jobs:
  include:
    - stage: deploy
      install: skip
      script: echo hello